replicaCount: 1
autoscaling: { enabled: false }
resources:
  requests: { cpu: 100m, memory: 256Mi }
  limits: { cpu: 250m, memory: 512Mi }
ingress:
  hosts: [{ host: api-dev.ecommerce.com, paths: [{ path: /, pathType: Prefix }] }]
