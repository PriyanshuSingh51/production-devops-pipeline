replicaCount: 3
autoscaling: { minReplicas: 3, maxReplicas: 10 }
resources:
  requests: { cpu: 250m, memory: 512Mi }
  limits: { cpu: 500m, memory: 1Gi }
ingress:
  hosts: [{ host: api.ecommerce.com, paths: [{ path: /, pathType: Prefix }] }]
